Звіт: Лабораторна робота з Vue, TypeScript і Router



1. Опис кроків, які було виконано.

А. Створення та запуск проєкту (виправлення критичних помилок)

Проєкт було створено за допомогою команди npm create vue@latest із вибором опцій TypeScript та Vue Router.

На етапі запуску було зафіксовано помилку Missing script: "dev", яку було усунуто шляхом додавання скрипту "dev": "vite" до файлу package.json.

Після запуску було отримано помилку HTTP ERROR 404. Встановлено, що маршрутизатор не міг знайти компоненти сторінок, оскільки була відсутня тека src/views. Цю теку було створено, і до неї було додано необхідні файли: HomeView.vue, AboutView.vue та ContactView.vue.

Було виявлено та виправлено синтаксичні помилки, пов'язані з дублюванням тегів у компонентах (ContactView.vue):

Здійснено об'єднання дубльованих блоків <script setup> та <template> в єдині, коректні блоки.


Б. Додавання сторінки "Контакти" та фінальне виправлення відображення

Було створено компонент UserCard.vue, у якому для визначення типів пропсів використано defineProps з TypeScript.

Новий маршрут /contact було додано до конфігурації маршрутизатора у файлі src/router/index.ts, а відповідне посилання (<RouterLink>) було додано до головного компонента App.vue.

На етапі фінальної перевірки було встановлено, що контент сторінок не відображається. Причиною виявилася відсутність ключового тегу <router-view /> у компоненті App.vue. Після його додавання відображення компонентів сторінок (включно з картками користувачів) було успішно відновлено.

2. Текст помилки типізації (Частина 4)

Типова помилка типізації, що виникає при спробі передати number (наприклад, 1) до пропсу, який очікує boolean:

// [vue-tsc] error TS2322: Type 'number' is not assignable to type 'boolean'.


Коментар, що ілюструє цю помилку, додається до файлу ContactView.vue:
<!-- <UserCard username="Student_1" :isActive="1" /> // Type 'number' is not assignable to type 'boolean'. -->


3. Висновки: що дає використання TypeScript та Vue Router у проєкті?

Використання цих інструментів забезпечує професійний рівень розробки та підвищує якість кінцевого продукту:

TypeScript (TS)

1. Дозволяє виявляти помилки невідповідності типів (як, наприклад, передача числа замість boolean) на етапі компіляції, запобігаючи помилкам під час виконання.

2. Забезпечує статичний аналіз коду, покращує автодоповнення в редакторі (IDE) та спрощує подальший рефакторинг проєкту.

Vue Router

1. Створює односторінковий застосунок, що забезпечує швидкі, безперервні переходи між сторінками, значно покращуючи користувацький досвід (UX).

2. Надає стандартизований та чіткий механізм для керування навігацією, мапуючи URL-адреси на відповідні компоненти сторінок.
